import type { Template } from "./types";

/**
 * Launch with an empty gallery. Sellers publish real Grok Bots via /submit.
 * Do not seed placeholder x.ai/bot URLs — most 404.
 */
export const SEED_TEMPLATES: Template[] = [];
